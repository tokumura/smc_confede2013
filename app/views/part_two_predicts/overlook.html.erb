<h2>みんなの予想（決勝トーナメント進出）</h2>

<div class="row">
  <div class="span10">
    <table class="table table-bordered table-hover">
      <thead>
        <tr>
          <td style="width:20%"></td>
          <td style="width:40%; text-align:center;">Group A</td>
          <td style="width:40%; text-align:center;">Group B</td>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>これが現実</td>
          <td>？？？・？？？</td>
          <td>？？？・？？？</td>
        </tr>
      <% @ptp.each do |p| %>
        <% user = User.find(p.user_id) %>
        <tr>
          <td><%=user.name%></td>
          <td>
            <%=Match.convert_country(p.team_a)%>・
            <%=Match.convert_country(p.team_b)%>
          </td>
          <td>
            <%=Match.convert_country(p.team_c)%>・
            <%=Match.convert_country(p.team_d)%>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>

