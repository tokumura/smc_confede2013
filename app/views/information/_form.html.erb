<%= form_for(@information) do |f| %>
  <% if @information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@information.errors.count, "error") %> prohibited this information from being saved:</h2>

      <ul>
      <% @information.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    タイトル<br/>
    <%= f.text_field :title, :style => "width:500px;" %>
  </div>
  <div class="field">
    本文<br/>
    <%= f.text_area :infobody, :style => "width:800px;" %>
  </div>
  <%= f.hidden_field :informant %>
  <div class="actions">
    <%= f.submit '登　録', :class => "btn btn-success" %>　
    <%= link_to '一覧に戻る', information_index_path, :class => 'btn btn-info' %>
  </div>
<% end %>
